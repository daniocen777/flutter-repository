import 'package:flutter/material.dart';

class SerieView extends StatelessWidget {
  const SerieView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SerieView'),
    );
  }
}
