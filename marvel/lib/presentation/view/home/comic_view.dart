import 'package:flutter/material.dart';

class ComicView extends StatelessWidget {
  const ComicView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ComicView'),
    );
  }
}
