import 'package:flutter/material.dart';

class ConfigTabPage extends StatelessWidget {
  const ConfigTabPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Configuración'),
      ),
    );
  }
}
