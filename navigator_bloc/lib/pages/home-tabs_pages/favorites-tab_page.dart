import 'package:flutter/material.dart';

class FavoritosTabPage extends StatelessWidget {
  const FavoritosTabPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Favoritos'),
      ),
    );
  }
}
